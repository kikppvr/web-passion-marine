# Copy to .env.production on the production server (not committed).
# Used by: npm run deploy:prd (copies to .env.local before build)

APP_ENV=production
NEXT_PUBLIC_SITE_URL=https://passionmarine.co.th

# SMTP (contact form) — set real values on the server
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=info@passionmarine.co.th
